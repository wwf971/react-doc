{
  "name": "test-readonly-fumadoc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "imports": {
    "#react-doc/*": "./*"
  },
  "scripts": {
    "dev": "pnpm -C frontend dev",
    "build": "pnpm -C frontend build",
    "start": "pnpm -C frontend preview"
  }
}
